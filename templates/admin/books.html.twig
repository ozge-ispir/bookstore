{% extends 'baseadmin.html.twig' %}

{% block body %}

<h3>Liste des livres:</h3>
    <ul>
        {% for book in books %}
            <li>
                "{{ book.title }}"  prix: {{ book.price }}€
                <a href="{{ path('admineditbook', {'id' : book.id })}}">Modifier ce livre</a>
                <a href="{{ path('adminremovebook', {'id' : book.id })}}">Supprimer ce livre</a>
            </li>
        {% endfor %}
    
    </ul>
</br>

{% endblock %}
