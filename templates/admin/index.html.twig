{% extends 'baseadmin.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h3>Bienvenue Admin</h3>
        </br>
        <a class="btn btn-primary btn-lg btn-block" href="{{ path('adminaddbook')}}">Ajouter un livre</a>
        </br>
        <a class="btn btn-secondary  btn-lg btn-block" href="{{ path('adminaddauthor')}}">Ajouter un auteur</a>
        </br>
        <a class="btn btn-primary btn-lg btn-block" href="{{ path('admininvoices')}}">Consulter les factures</a>
        </br>
        <a class="btn btn-secondary  btn-lg btn-block" href="{{ path('adminshowbooks')}}">Modifier les livres</a>
        </br>
        <a class="btn btn-primary btn-lg btn-block" href="{{ path('adminauthors')}}">Modifier les auteurs</a>
        </br>
    </div>
{% endblock %}
